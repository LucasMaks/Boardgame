<UserControl x:Class="Boardgame.View.StartPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Boardgame.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="630" d:DesignWidth="1040">

    <UserControl.DataContext>
        <vm:StarPageVM/>
    </UserControl.DataContext>

    <Grid>
        <!--// Base Grid //-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--Navigation Menu-->
        <Border    Grid.Column="0" CornerRadius="10">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{StaticResource secondaryBackColor1}" Offset="0"/>
                    <GradientStop Color="{StaticResource secondaryBackColor2}" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>
        <Grid>
            <Border MaxWidth="250" MaxHeight="250" HorizontalAlignment="Center" Width="250" Height="250" VerticalAlignment="Center" Margin="0,0,0,-5">
                <Border.Background>
                    <ImageBrush ImageSource="G:\BoardGame\Boardgame\Image\StartPage.png" Stretch="Uniform"/>
                </Border.Background>
            </Border>

            <TextBlock Text="Board Game"
           FontSize="36"
           FontWeight="Bold"   RenderTransformOrigin="0.5,0.5" Width="206" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,270,0,0">
                <TextBlock.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleY="0.273"/>
                        <RotateTransform/>
                        <TranslateTransform Y="0.495"/>
                    </TransformGroup>
                </TextBlock.RenderTransform>
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="1,9" EndPoint="0,2">
                        <GradientStop Color="{StaticResource winBorderColor6}" Offset="0"/>
                        <GradientStop Color="{StaticResource winBorderColor7}" Offset="1"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>

        </Grid>
    </Grid>
</UserControl>
